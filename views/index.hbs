<!DOCTYPE html>
<html>
<head> 
    <title> ToDo App </title>
    <link href="https://fonts.googleapis.com/css?family=Lato:100" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link href="public\css\style.css" type="text/css" rel="stylesheet">

</head>

<body>
    <div class="jumbotron">
        <h2 style="text-align: center; padding-bottom: 5%"> Salient To Do List App </h2>

    <div class="row">
            <div class="form-group col">
                <h3>Add a new task</h3>
                <form action="/addtask" method="POST">
                    <input type="text" class="form-control" name="newtask" placeholder="Add new task"> <br>
                    <button class="btn btn-primary"> Add Task </button>
                </form>
            </div>

        <div class="col">

        <h3> Pending Tasks </h3>

        <form class="form-group" action="/markcompleted" method="POST">
            <ul class="list-group">
                {{#each tasks}}
                    {{#unless this.Completed}}
                        <li class="list-group-item">
                            <input type="checkbox" name="completedtasks" value="{{this._id}}">
                            {{this.Task}}
                        </li>
                    {{/unless}}
                {{/each}}
            </ul>

            <button style="margin-top: 5%;"  class="btn btn-success"> Mark Completed </button>
        </form>
        </div>

        <div class="col">
            <h3> Completed Tasks </h3>

            <ul class="list-group">
                {{#each tasks}}
                    {{#if this.Completed}}
                        <li class="list-group-item">
                            {{this.Task}} 
                            <form action="/deletetask" method="post" style="display: inline;">
                                <input type="hidden" name="taskID" value="{{this._id}}">
                                <button name="deleteTask" type="submit" style="float: right;" class="btn btn-danger">Delete</button>
                            </form>
                        </li>
                    {{/if}}
                {{/each}}
            </ul>
        </div>

</div>





    </div>
</body>
</html>